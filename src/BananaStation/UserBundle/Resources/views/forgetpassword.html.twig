{% extends "::user.html.twig" %}
{% block body %}
    <section>
        <h2>Récupération de mot de passe</h2>
        {{ form_start(form, {'method': 'POST', 'attr': {'class': 'form-horizontal'} }) }}
        <p>Pour récupérer votre mot de passe, il vous faut entrer l'adresse email qui correspond à votre compte. Un mail vous sera envoyé vous indiquant la procédure à suivre pour le récupérer.</p>
        <div class="form-group">
            <label class="control-label">Adresse email :</label>
            {{ form_widget(form.email, { 'attr': {'class': 'form-control', 'placeholder': 'Adresse email'} }) }}
        </div>
        <div class="form-group">
            <label class="control-label">Complétez le captcha :</label>
            {{ form_widget(form.recaptcha) }}
        </div>
        <div class="form-group">
            <input type="submit" name="valider" value="Valider"/>
        </div>
        {{ form_end(form) }}
    </section>
{% endblock %}